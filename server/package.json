{
  "name": "aplus-content-creator-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Express backend for the Amazon A+ Content Creator app — proxies frontend requests to the Anthropic API so the API key never reaches the browser.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.66.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
